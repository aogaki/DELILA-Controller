import { GroupMaskPipe } from './group-mask.pipe';

describe('GroupMaskPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupMaskPipe();
    expect(pipe).toBeTruthy();
  });
});
